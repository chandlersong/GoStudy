# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go tests -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
/.idea
build
/tests/aws/lambda/dynamodb/vendor/
/tests/aws/sam/first-app/aws-lambda/aws-lambda
/tests/aws/sam/first-app/dlv/
vendor/
